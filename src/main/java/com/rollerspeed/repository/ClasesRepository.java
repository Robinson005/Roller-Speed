package com.rollerspeed.repository;

import com.rollerspeed.model.Clases;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ClasesRepository extends JpaRepository<Clases, Long> {
}
